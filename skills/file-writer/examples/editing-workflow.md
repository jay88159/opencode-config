# 编辑工作流示例

本文档展示 file-writer skill 的典型工作流和工具调用方式。

---

## 示例 1：创建并迭代编辑文章

### 场景
用户要写一篇技术博客，需要多次修改。

### 第一轮：创建文件

**用户请求**：帮我写一篇关于 React Hooks 的文章

**操作流程**：
```
1. 确认保存路径 → ~/Documents/react-hooks-guide.md
2. 使用 Write 工具创建文件
3. 写入初始内容
```

**工具调用**：
```
Write(
  path: "~/Documents/react-hooks-guide.md",
  contents: "# React Hooks 完全指南\n\n## 简介\n\nReact Hooks 是..."
)
```

### 第二轮：修改第二段

**用户请求**：把第二段改得更详细一些

**操作流程**：
```
1. Read 读取文件，了解当前结构
2. 定位第二段位置（假设是第 5-10 行）
3. 使用 StrReplace 替换该段落
```

**工具调用**：
```
# Step 1: 读取文件
Read(path: "~/Documents/react-hooks-guide.md")

# Step 2: 定位后使用 StrReplace
StrReplace(
  path: "~/Documents/react-hooks-guide.md",
  old_string: "## 简介\n\nReact Hooks 是 React 16.8 引入的新特性。",
  new_string: "## 简介\n\nReact Hooks 是 React 16.8 版本引入的革命性特性，它允许你在函数组件中使用 state 和其他 React 特性，而无需编写 class 组件。\n\nHooks 的出现解决了几个长期困扰 React 开发者的问题：组件间状态逻辑复用困难、复杂组件难以理解、class 组件的 this 指向问题等。"
)
```

### 第三轮：在结尾追加内容

**用户请求**：在文章结尾加一个总结

**操作流程**：
```
1. Read 读取文件末尾内容
2. 找到最后一段作为定位锚点
3. 在其后插入总结
```

**工具调用**：
```
# 假设文件最后几行是 "...这就是 useEffect 的基本用法。"
StrReplace(
  path: "~/Documents/react-hooks-guide.md",
  old_string: "这就是 useEffect 的基本用法。",
  new_string: "这就是 useEffect 的基本用法。\n\n## 总结\n\nReact Hooks 彻底改变了我们编写 React 组件的方式。通过 useState、useEffect、useContext 等 Hooks，我们可以用更简洁、更可维护的代码实现相同的功能。"
)
```

---

## 示例 2：按行号精确修改

### 场景
用户指定要修改特定行。

**用户请求**：把第 15 行改成 "const [count, setCount] = useState(0);"

**操作流程**：
```
1. Read 读取文件，确认第 15 行内容
2. 使用第 15 行的完整内容作为 old_string
3. StrReplace 替换
```

**工具调用**：
```
# Step 1: 读取确认
Read(path: "~/Documents/react-hooks-guide.md", offset: 12, limit: 6)

# 假设第 15 行是 "const [count] = useState();"
# Step 2: 替换（包含上下文以确保唯一）
StrReplace(
  path: "~/Documents/react-hooks-guide.md",
  old_string: "function Counter() {\n  const [count] = useState();\n  return <div>{count}</div>;",
  new_string: "function Counter() {\n  const [count, setCount] = useState(0);\n  return <div>{count}</div>;"
)
```

**关键点**：
- `old_string` 包含了第 14、15、16 行，确保唯一匹配
- 只修改第 15 行的内容，保持 14 和 16 行不变

---

## 示例 3：搜索定位后修改

### 场景
用户描述要修改的内容，但不知道行号。

**用户请求**：把文章里所有的 "组件" 改成 "Component"

**操作流程**：
```
1. Grep 搜索所有 "组件" 出现的位置
2. 决定是全局替换还是逐个处理
3. 使用 StrReplace 的 replace_all 参数
```

**工具调用**：
```
# Step 1: 搜索确认
Grep(pattern: "组件", path: "~/Documents/react-hooks-guide.md")

# Step 2: 全局替换
StrReplace(
  path: "~/Documents/react-hooks-guide.md",
  old_string: "组件",
  new_string: "Component",
  replace_all: true
)
```

---

## 示例 4：删除特定段落

### 场景
用户要删除文章中的某一节。

**用户请求**：删除 "## 常见问题" 这一节

**操作流程**：
```
1. Read 读取文件，找到该节的起止位置
2. 确定从 "## 常见问题" 到下一个 "##" 之前的全部内容
3. 使用 StrReplace 删除（new_string 为空或仅保留必要空行）
```

**工具调用**：
```
# 假设 "## 常见问题" 节内容如下
StrReplace(
  path: "~/Documents/react-hooks-guide.md",
  old_string: "\n## 常见问题\n\n### Q1: Hooks 能在 class 组件中使用吗？\n\n不能，Hooks 只能在函数组件中使用。\n\n### Q2: 可以自定义 Hooks 吗？\n\n可以，自定义 Hooks 是复用状态逻辑的推荐方式。\n",
  new_string: "\n"
)
```

---

## 示例 5：在指定位置插入新内容

### 场景
用户要在某个标题下方添加新内容。

**用户请求**：在 "## useState" 标题下面加一段说明

**操作流程**：
```
1. Grep 或 Read 找到 "## useState" 的位置
2. 将 "## useState" 和其后的第一段作为 old_string
3. 在标题和原内容之间插入新段落
```

**工具调用**：
```
StrReplace(
  path: "~/Documents/react-hooks-guide.md",
  old_string: "## useState\n\nuseState 是最基础的 Hook。",
  new_string: "## useState\n\n> 💡 useState 是 React 中最常用的 Hook，几乎每个函数组件都会用到它。\n\nuseState 是最基础的 Hook。"
)
```

---

## 关键原则总结

| 原则 | 说明 |
|------|------|
| **先读后改** | 任何编辑前必须 Read 文件，了解当前内容和结构 |
| **old_string 要唯一** | 包含 3-5 行上下文，确保不会误匹配其他位置 |
| **保持格式** | new_string 中的缩进、空行要与原文风格一致 |
| **验证结果** | 复杂编辑后可选择性 Read 验证修改是否正确 |
